interface:
  display_name: "PR Response"
  short_description: "GitHub PR のレビュー返信と対応を進める"
  default_prompt: "Use $github-pr-response to inspect GitHub PR feedback, classify actionable comments, implement fixes, then prepare replies, resolves, and re-review steps in that order."
