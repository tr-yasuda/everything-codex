interface:
  display_name: "TDD"
  short_description: "RED → GREEN → REFACTOR で変更を進める"
  default_prompt: "$tdd を使い、1つの振る舞いごとに RED → GREEN → REFACTOR を完了しながら実装してください。"

policy:
  allow_implicit_invocation: true
